# Local Docker practice only. Generate two different values with: openssl rand -hex 32
LOCAL_ADMIN_PASSWORD=REPLACE_WITH_A_GENERATED_HEX_PASSWORD
LOCAL_APP_PASSWORD=REPLACE_WITH_A_DIFFERENT_GENERATED_HEX_PASSWORD
